base = int(input('Enter Base: '))
height = int(input('Enter Height: '))
area = 0.5 * base * height

print('The area of rectangle with a base of {} and a height of {} is {}'.format(base, height, area))
